ingress:
  hosts:
    - host: immich-dev.internal
      paths:
        - path: /
          pathType: Prefix

postgresql:
  auth:
    existingSecret: immich-postgresql-auth
valkey:
  external:
    existingSecret: immich-valkey-auth