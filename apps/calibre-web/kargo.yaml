chart:
  targetRevision: 0.0.49
kargo:
  warehouse:
    image:
      imageSelectionStrategy: Lexical
      repoURL: ghcr.io/crocodilestick/calibre-web-automated
      constraint: ''
      allowTags: '^v\d+\.\d+\.\d+$'
      ignoreTags:
        - '.*-b\.\d+$'
        - 'nightly.*$'
        - 'latest'
        - 'dev-.*'
        - 'version-.*$'
        - 'amd64-.*$'
      discoveryLimit: 15
    chart:
      repoURL: https://squallium.github.io/kubernetes-at-home
      name: calibre-web
extSecrets:
  - name: github-ssh
    namespace: calibre-web
    labels:
      kargo.akuity.io/cred-type: git
    secrets:
      - secretKey: repoURL
        remoteRef:
          key: secret/kargo/github
          property: repo_url
      - secretKey: sshPrivateKey
        remoteRef:
          key: secret/kargo/github
          property: ssh_private_key
  - name: ghcr
    namespace: calibre-web
    labels:
      kargo.akuity.io/cred-type: image
    secrets:
      - secretKey: repoURL
        remoteRef:
          key: secret/kargo/ghcr
          property: repo_url
      - secretKey: repoURLIsRegex
        remoteRef:
          key: secret/kargo/ghcr
          property: repo_url_is_regex
      - secretKey: username
        remoteRef:
          key: secret/kargo/ghcr
          property: username
      - secretKey: password
        remoteRef:
          key: secret/kargo/ghcr
          property: password