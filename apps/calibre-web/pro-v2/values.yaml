image:
  tag: 0.6.27

volumeMounts:
  - name: config
    mountPath: /config
  - name: books
    mountPath: /books

volumes:
  - name: config
    persistentVolumeClaim:
      claimName: calibre-web-config

  - name: books
    hostPath:
      path: /mnt/services/calibre-web/calibre-library
      type: Directory

ingress:
  hosts:
    - host: calibre-web.internal
      paths:
        - path: /
          pathType: Prefix

storage:
  enabled: true
  provisioner:
    enabled: false

  persistentVolumeClaims:
    - name: calibre-web-config
      size: 1Gi