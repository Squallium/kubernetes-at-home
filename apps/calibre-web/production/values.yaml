image:
  tag: 0.6.27
deployment:
  additionalVolumeMounts:
    - name: calibre-books
      mountPath: /calibre-library
    - name: calibre-ingest
      mountPath: /cwa-book-ingest
  additionalVolumes:
    - name: calibre-books
      hostPathPath: calibre-library
    - name: calibre-ingest
      hostPathPath: ingest
ingress:
  hostname: calibre-web.internal
