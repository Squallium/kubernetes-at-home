api:
  host: kargo.internal
  service:
    type: ClusterIP
  ingress:
    enabled: true
    ingressClassName: nginx
  tls:
    enabled: true
    selfSignedCert: true
    secretName: kargo-api-tls
    hosts:
      - kargo.internal
