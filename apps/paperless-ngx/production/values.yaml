ingress:
  hostname: paperless.internal

deployment:
  additionalEnv:
    PAPERLESS_CSRF_TRUSTED_ORIGINS: https://paperless-dev.internal
  resources:
    requests:
      cpu: 1000m
      memory: 2Gi
    limits:
      cpu: 300m
      memory: 512Mi