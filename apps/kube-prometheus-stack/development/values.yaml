grafana:
  enabled: false

alertmanager:
  enabled: false

defaultRules:
  create: false

kubeEtcd:
  enabled: false

kubeControllerManager:
  enabled: false

kubeScheduler:
  enabled: false

kubeProxy:
  enabled: false

kubeApiServer:
  enabled: false

coreDns:
  enabled: false

kubeStateMetrics:
  enabled: true

nodeExporter:
  enabled: true

prometheus:
  ingress:
    enabled: true
    ingressClassName: public
    hosts:
      - prometheus-dev.internal
    paths:
      - /
    pathType: Prefix

  prometheusSpec:
    replicas: 1

    enableRemoteWriteReceiver: false

    scrapeInterval: 60s
    evaluationInterval: 60s

    retention: 12h

    serviceMonitorSelectorNilUsesHelmValues: false
    podMonitorSelectorNilUsesHelmValues: false
    serviceMonitorSelector: {}
    podMonitorSelector: {}

    scrapeClasses:
      - default: true
        name: cluster-relabeling
        relabelings:
          - sourceLabels: [ __name__ ]
            regex: (.*)
            targetLabel: cluster
            replacement: dev
            action: replace

    storageSpec:
      volumeClaimTemplate:
        spec:
          accessModes: [ "ReadWriteOnce" ]
          resources:
            requests:
              storage: 2Gi
          storageClassName: "microk8s-hostpath"

    additionalScrapeConfigs: []

    resources:
      requests:
        cpu: 50m
        memory: 128Mi
      limits:
        cpu: 200m
        memory: 256Mi

    remoteWrite:
      - url: http://prometheus.internal/api/v1/write
        writeRelabelConfigs:
          - sourceLabels: [ namespace ]
            regex: authentik|calibre-web|cloudflared|home-assistant|jellyfin|kavita|lidarr|paperless-ngx|pgbackweb|prometheus|prowlarr|radarr|shoko|sonarr|vikunja|warracker
            action: keep
          - sourceLabels: [__name__]
            regex: (.*)
            targetLabel: cluster
            replacement: dev
            action: replace

prometheusOperator:
  admissionWebhooks:
    enabled: false
  tls:
    enabled: false

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 256Mi