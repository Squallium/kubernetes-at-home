zigbee2mqtt:
  serial:
    port: "/dev/ttyUSB0"
    adapter: "zstack"
ingress:
  hosts:
    - host: zigbee2mqtt.internal
      paths:
        - path: /
          pathType: Prefix

statefulset:
  resources:
    requests:
      memory: 150Mi
      cpu: 200m
    limits:
      memory: 300Mi
      cpu: 200m

#zigbee2mqtt:
#  external_converters:
#    - aqara-t2.js
