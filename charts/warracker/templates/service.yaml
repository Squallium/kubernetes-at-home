apiVersion: v1
kind: Service
metadata:
  name: warracker
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.servicePort }}
      targetPort: {{ .Values.deployment.containerPort }}
  selector:
    app: warracker
